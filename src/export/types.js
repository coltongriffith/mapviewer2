export function createScene({ width, height, layers, layout, map, container, project, template }) {
  return {
    width,
    height,
    layers,
    layout,
    map,
    container,
    project,
    template,
  };
}
